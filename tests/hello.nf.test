nextflow_process {

    name "Test SAY_HELLO"
    script "./hello.nf"
    process "SAY_HELLO"

    test("outputs hello file") {
        when {
            process {
                """
                """
            }
        }
        then {
            assert process.success
            assert snapshot(process.out).match()
        }
    }
}
