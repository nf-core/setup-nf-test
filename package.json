{
  "name": "setup-nf-test",
  "version": "0.1.0",
  "description": "",
  "main": "dist/setup/index.js",
  "scripts": {
    "build": "ncc build src/setup.js -o dist/setup --license licenses.txt && ncc build src/post.js -o dist/setup --license licenses.txt",
    "prepare": "husky"
  },
  "keywords": [
    "actions",
    "setup",
    "nf-test"
  ],
  "author": "nf-core",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@actions/tool-cache": "^2.0.1",
    "@vercel/ncc": "^0.38.1"
  },
  "devDependencies": {
    "@actions/cache": "^3.2.4",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5"
  },
  "lint-staged": {
    "*.{ts,js,yml,md,json}": "prettier --write"
  }
}
